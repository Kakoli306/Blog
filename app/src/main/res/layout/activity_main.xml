<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="16dp"
    android:paddingRight="16dp"
    android:paddingBottom="16dp"
    tools:context="com.example.kakoli.blog.MainActivity">

  <Button
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="Show Notificaton"
      android:id="@+id/showNotificationBut"
      android:layout_alignParentTop="true"
      android:layout_alignParentLeft="true"
      android:layout_alignParentStart="true"
      android:onClick="showNotification"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Stop Notificaton"
        android:id="@+id/stopNotificationBut"

        android:layout_below="@+id/showNotification"
        android:layout_alignParentStart="true"
        android:layout_marginTop="12dp"
        android:onClick="stopNotification" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Alert in 5 Seconds"
        android:id="@+id/alertButton"
        android:onClick="setAlarm"
        android:layout_marginTop="79dp"
        android:layout_below="@+id/showNotification"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
